// Update this after the backend service deploys on Render, if the assigned
// URL differs from the predictable "<service-name>.onrender.com" pattern
// (Render appends a suffix if the name is already taken).
window.VOICEORIGIN_API_BASE = "https://voiceorigin-api.onrender.com";
